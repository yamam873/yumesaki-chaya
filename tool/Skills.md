# 夢前茶屋 (Yumesaki Chaya) テーマ開発 Skills & Rules

このファイルは、「和モダンな古民家カフェ」というコンセプトを体現し、一貫性のある美しいコードを書くための共通ルールです。

## 1. デザインシステム・トンマナの絶対ルール

### カラーパレットの厳守
* **ベース**: 温かみのある土壁のようなオフホワイト/アイボリー (`bg-surface`, `bg-background`)
* **テキスト**: 真っ黒は使用禁止。古い梁のような焦げ茶や墨色 (`text-on-surface`, `text-primary`) を使用する。
* **アクセント**: プリンのカラメルを思わせる琥珀色、お茶を思わせる深い緑 (`secondary`) をポイントで使用する。

### タイポグラフィ
* 見出しは必ず上品な明朝体（`font-headline-...`）、本文は読みやすいゴシック体（`font-body-...`）を使い分ける。

### 余白（ホワイトスペース）
* 「古民家のくつろぎ」を表現するため、各セクションや要素間の余白は通常より広めに（たっぷり）とる。

---

## 2. コーディング・命名規則（標準CSSの利用）

### 分かりやすいクラス名（BEM記法の活用）
* 後から見返しても混乱しないよう、Tailwindなどのフレームワークには依存せず、標準的なCSS（またはSCSS）を使用する。
* `p-concept__title` のように、「どこで」「何に使われているか」が直感的にわかる命名規則（BEMなどの概念）を意識し、管理しやすいコードにする。

### CSS変数の利用
* サイト全体で使うカラー（メインの琥珀色や土壁の色など）やフォント設定は、CSSのカスタムプロパティ（`--color-primary` など）として定義する。
* カラーコードの直書き（ハードコード）を避けることで、後からの色変更を容易にする。

### コンポーネントの再利用
* ボタンやカードデザインなど、複数ページで使い回す要素は共通のクラス名（例：`.c-button`, `.c-card`）を付与し、CSSでスタイルを一元管理する。

---

## 3. UI/UX・アニメーション（心地よい動き）

### 和モダンなトランジション
* ホバー効果やモーダルの開閉などは、機敏すぎる動きを避け、CSSの `transition` を使い、ゆっくりと滑らかに変化（フェード）させる。

### スクロール・リビール
* コンテンツは一気に表示させず、スクロールに合わせて下からフワッと浮かび上がる上品なアニメーションを全ページで統一して使用する。

---

## 4. 将来のWordPress化を見据えたHTML設計

### セマンティックなマークアップ
* `<div>` の乱用を避け、`<header>`, `<main>`, `<section>`, `<article>`, `<footer>` などの意味のあるHTMLタグを適切に使用する。

### 共通パーツの意識
* 現在は静的HTMLだが、後から `header.php` や `footer.php` に分割しやすいよう、全ページでヘッダーとフッターのコードは完全に同一に保つ。

### 相対パスの利用
* 画像やリンクのパスは、将来動的に変更しやすいように構成を意識する。

---

## 5. ワークフロー

### スモールステップでの確認
* 一気にページ全体を書き換えず、「ヒーローセクションの修正」「モーダルの実装」など、1つの要素が完成するごとにプレビューで表示と挙動を確認し、問題がなければ次へ進む。
